# CLI App

This directory is reserved for CLI-facing application wiring and shell-specific packaging concerns.

The Python package entrypoint currently lives in `src/svg_scrapling/` and will be expanded in issue `#2`.

